#write the function to split a dataset into train and test
#1)default spilt rato=70%
#2)sampling method 

splitdata=function(df,ratio)
{
  train
    test
    ret=c()
  ret[1]=data.frame(list(train))
  ret[2]=data.frame(list(test))
  return(ret)
  
}
dat=splitdata(datframe)
train=dat[1]; test=dat[2]
#ret=splitdata(mydtaframe)
#train=ret[1]
#test=ret[2]

#dim(train)
